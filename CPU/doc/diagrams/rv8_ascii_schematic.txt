RV8 CPU — Chip Wiring Diagram (ASCII)
======================================

═══════════════════════════════════════════════════════════════════════════════
 PROGRAM COUNTER (16-bit, counts up each clock)
═══════════════════════════════════════════════════════════════════════════════

  U1 (74HC161)       U2 (74HC161)       U3 (74HC161)       U4 (74HC161)
  PC[3:0]            PC[7:4]            PC[11:8]           PC[15:12]
 ┌────────────┐     ┌────────────┐     ┌────────────┐     ┌────────────┐
 │CP  ← CLK   │     │CP  ← CLK   │     │CP  ← CLK   │     │CP  ← CLK   │
 │/MR ← RST_N │     │/MR ← RST_N │     │/MR ← RST_N │     │/MR ← RST_N │
 │/PE ← PC_LD │     │/PE ← PC_LD │     │/PE ← PC_LD │     │/PE ← PC_LD │
 │CEP ← PC_INC│     │CEP ← PC_INC│     │CEP ← PC_INC│     │CEP ← PC_INC│
 │CET ← PC_INC│     │CET ←─┐     │     │CET ←─┐     │     │CET ←─┐     │
 │D0  ← D0    │     │D0  ← D4    │     │D0  ← D8    │     │D0  ← D12   │
 │D1  ← D1    │     │D1  ← D5    │     │D1  ← D9    │     │D1  ← D13   │
 │D2  ← D2    │     │D2  ← D6    │     │D2  ← D10   │     │D2  ← D14   │
 │D3  ← D3    │     │D3  ← D7    │     │D3  ← D11   │     │D3  ← D15   │
 │Q0  → PC0   │     │Q0  → PC4   │     │Q0  → PC8   │     │Q0  → PC12  │
 │Q1  → PC1   │     │Q1  → PC5   │     │Q1  → PC9   │     │Q1  → PC13  │
 │Q2  → PC2   │     │Q2  → PC6   │     │Q2  → PC10  │     │Q2  → PC14  │
 │Q3  → PC3   │     │Q3  → PC7   │     │Q3  → PC11  │     │Q3  → PC15  │
 │TC  →────────┼──►──┘TC  →───────┼──►──┘TC  →───────┼──►──┘TC          │
 └────────────┘     └────────────┘     └────────────┘     └────────────┘

═══════════════════════════════════════════════════════════════════════════════
 POINTER (16-bit, auto-increment for memory access)
═══════════════════════════════════════════════════════════════════════════════

  U11 (74HC161)      U12 (74HC161)
  PTR low            PTR high
 ┌────────────┐     ┌────────────┐
 │CP  ← CLK   │     │CP  ← CLK   │
 │/MR ← RST_N │     │/MR ← RST_N │
 │/PE ← PL_LD │     │/PE ← PH_LD │
 │CEP ← PTR_INC     │CEP ← PTR_INC
 │CET ← PTR_INC     │CET ←─┐     │
 │D0  ← D0    │     │D0  ← D0    │
 │D1  ← D1    │     │D1  ← D1    │
 │D2  ← D2    │     │D2  ← D2    │
 │D3  ← D3    │     │D3  ← D3    │
 │Q0  → PTR0  │     │Q0  → PTR4  │
 │Q1  → PTR1  │     │Q1  → PTR5  │
 │Q2  → PTR2  │     │Q2  → PTR6  │
 │Q3  → PTR3  │     │Q3  → PTR7  │
 │TC  →────────┼──►──┘             │
 └────────────┘     └────────────┘

═══════════════════════════════════════════════════════════════════════════════
 ADDRESS MUX (selects PC or Pointer → address bus)
═══════════════════════════════════════════════════════════════════════════════

  U16 (74HC157)      U17 (74HC157)      U25 (74HC157)
  Addr[3:0]          Addr[7:4]          Clock Mux
 ┌────────────┐     ┌────────────┐     ┌────────────┐
 │S   ← ADDR_SEL    │S   ← ADDR_SEL    │S   ← RUN_N  │
 │/E  ← GND   │     │/E  ← GND   │     │/E  ← GND   │
 │I0a ← PC0   │     │I0a ← PC4   │     │I0a ← OSC   │
 │I1a ← PTR0  │     │I1a ← PTR4  │     │I1a ← STEP  │
 │Za  → A0    │     │Za  → A4    │     │Za  → CLK   │
 │I0b ← PC1   │     │I0b ← PC5   │     │I0b ← GND   │
 │I1b ← PTR1  │     │I1b ← PTR5  │     │I1b ← GND   │
 │Zb  → A1    │     │Zb  → A5    │     │Zb  → (nc)  │
 │I0c ← PC2   │     │I0c ← PC6   │     │I0c ← GND   │
 │I1c ← PTR2  │     │I1c ← PTR6  │     │I1c ← GND   │
 │Zc  → A2    │     │Zc  → A6    │     │Zc  → (nc)  │
 │I0d ← PC3   │     │I0d ← PC7   │     │I0d ← GND   │
 │I1d ← PTR3  │     │I1d ← PTR7  │     │I1d ← GND   │
 │Zd  → A3    │     │Zd  → A7    │     │Zd  → (nc)  │
 └────────────┘     └────────────┘     └────────────┘

═══════════════════════════════════════════════════════════════════════════════
 INSTRUCTION REGISTER (latches opcode + operand from data bus)
═══════════════════════════════════════════════════════════════════════════════

  U5 (74HC574)       U6 (74HC574)
  IR Opcode          IR Operand
 ┌────────────┐     ┌────────────┐
 │Cp  ← IR0_CLK     │Cp  ← IR1_CLK
 │/OE ← GND   │     │/OE ← GND   │
 │D0  ← D0    │     │D0  ← D0    │
 │D1  ← D1    │     │D1  ← D1    │
 │D2  ← D2    │     │D2  ← D2    │
 │D3  ← D3    │     │D3  ← D3    │
 │D4  ← D4    │     │D4  ← D4    │
 │D5  ← D5    │     │D5  ← D5    │
 │D6  ← D6    │     │D6  ← D6    │
 │D7  ← D7    │     │D7  ← D7    │
 │Q0  → OP0   │     │Q0  → IMM0  │
 │Q1  → OP1   │     │Q1  → IMM1  │
 │Q2  → OP2   │     │Q2  → IMM2  │
 │Q3  → OP3   │     │Q3  → IMM3  │
 │Q4  → OP4   │     │Q4  → IMM4  │
 │Q5  → OP5   │     │Q5  → IMM5  │
 │Q6  → OP6   │     │Q6  → IMM6  │
 │Q7  → OP7   │     │Q7  → IMM7  │
 └────────────┘     └────────────┘

═══════════════════════════════════════════════════════════════════════════════
 REGISTERS (a0=accumulator, t0=temp, sp=stack, pg=page)
═══════════════════════════════════════════════════════════════════════════════

  U7 (74HC574)    U8 (74HC574)    U9 (74HC574)    U10 (74HC574)
  a0 (accum)      t0 (temp)       sp (stack)      pg (page)
 ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
 │Cp ← A0_CLK    │Cp ← T0_CLK    │Cp ← SP_CLK    │Cp ← PG_CLK
 │/OE← GND  │    │/OE← GND  │    │/OE← GND  │    │/OE← GND  │
 │D0 ← ALU_R0    │D0 ← D0   │    │D0 ← D0   │    │D0 ← D0   │
 │D1 ← ALU_R1    │D1 ← D1   │    │D1 ← D1   │    │D1 ← D1   │
 │...← ALU_R.    │...← D... │    │...← D... │    │...← D... │
 │D7 ← ALU_R7    │D7 ← D7   │    │D7 ← D7   │    │D7 ← D7   │
 │Q0 → A00  │    │Q0 → T00  │    │Q0 → SP0  │    │Q0 → PG0  │
 │Q1 → A01  │    │Q1 → T01  │    │Q1 → SP1  │    │Q1 → PG1  │
 │...→ A0.  │    │...→ T0.  │    │...→ SP.  │    │...→ PG.  │
 │Q7 → A07  │    │Q7 → T07  │    │Q7 → SP7  │    │Q7 → PG7  │
 └──────────┘    └──────────┘    └──────────┘    └──────────┘

═══════════════════════════════════════════════════════════════════════════════
 ALU (8-bit add/subtract with carry chain)
═══════════════════════════════════════════════════════════════════════════════

  U15 (74HC86)       U13 (74HC283)      U14 (74HC283)
  XOR (B invert)     Adder low          Adder high
 ┌────────────┐     ┌────────────┐     ┌────────────┐
 │1A ← IMM0   │     │A1 ← A00   │     │A1 ← A04   │
 │1B ← SUB    │     │A2 ← A01   │     │A2 ← A05   │
 │1Y → ALU_B0 │     │A3 ← A02   │     │A3 ← A06   │
 │2A ← IMM1   │     │A4 ← A03   │     │A4 ← A07   │
 │2B ← SUB    │     │B1 ← ALU_B0│     │B1 ← ALU_B4│
 │2Y → ALU_B1 │     │B2 ← ALU_B1│     │B2 ← ALU_B5│
 │3A ← IMM2   │     │B3 ← ALU_B2│     │B3 ← ALU_B6│
 │3B ← SUB    │     │B4 ← ALU_B3│     │B4 ← ALU_B7│
 │3Y → ALU_B2 │     │C0 ← SUB   │     │C0 ←─┐     │
 │4A ← IMM3   │     │S1 → ALU_R0│     │S1 → ALU_R4│
 │4B ← SUB    │     │S2 → ALU_R1│     │S2 → ALU_R5│
 │4Y → ALU_B3 │     │S3 → ALU_R2│     │S3 → ALU_R6│
 └────────────┘     │S4 → ALU_R3│     │S4 → ALU_R7│
                    │C4 →────────┼──►──┘C4 → COUT  │
                    └────────────┘     └────────────┘

═══════════════════════════════════════════════════════════════════════════════
 CONTROL LOGIC
═══════════════════════════════════════════════════════════════════════════════

  U18 (74HC138)      U24 (74HC138)      U19 (74HC245)
  Unit Decode        Address Decode     Bus Buffer
 ┌────────────┐     ┌────────────┐     ┌────────────┐
 │A0 ← OP5    │     │A0 ← A13   │     │DIR ← DIR   │
 │A1 ← OP6    │     │A1 ← A14   │     │/CE ← BUF_N │
 │A2 ← OP7    │     │A2 ← A15   │     │A0  ← DINT0 │
 │E2 ← VCC    │     │E2 ← VCC   │     │A1  ← DINT1 │
 │/E0← GND    │     │/E0← GND   │     │... ← DINT. │
 │/E1← EXEC_N │     │/E1← GND   │     │A7  ← DINT7 │
 │/Y0→ ALU_N  │     │/Y0→ RAM0_N│     │B0  → D0    │
 │/Y1→ IMM_N  │     │/Y1→ RAM1_N│     │B1  → D1    │
 │/Y2→ LDST_N │     │/Y4→ IO_N  │     │... → D...  │
 │/Y3→ BR_N   │     │/Y6→ ROM6_N│     │B7  → D7    │
 │/Y4→ SH_N   │     │/Y7→ ROM7_N│     └────────────┘
 │/Y5→ PTR_N  │     └────────────┘
 │/Y7→ SYS_N  │
 └────────────┘

  U20 (74HC74)       U21 (74HC74)       U22 (74HC08)    U23 (74HC32)
  Flags Z,C          Flag N + State     AND gates       OR gates
 ┌────────────┐     ┌────────────┐     ┌──────────┐   ┌──────────┐
 │FF1.D←ALU_Z │     │FF1.D←ALU_N │     │1: IR0_CLK│   │1: ROM_N  │
 │FF1.C←FL_CLK│     │FF1.C←FL_CLK│     │2: IR1_CLK│   │2: RAM_N  │
 │FF1.Q→FLAG_Z│     │FF1.Q→FLAG_N│     │3: A0_CLK │   │3: (ctrl) │
 │FF2.D←ALU_C │     │FF2.D←(next)│     │4: (ctrl) │   │4: (ctrl) │
 │FF2.C←FL_CLK│     │FF2.C←CLK   │     └──────────┘   └──────────┘
 │FF2.Q→FLAG_C│     │FF2.Q→STATE │
 └────────────┘     └────────────┘

═══════════════════════════════════════════════════════════════════════════════
 MEMORY
═══════════════════════════════════════════════════════════════════════════════

  U26 (AT28C256)                    U27 (62256 SRAM)
  ROM 32KB ($C000-$FFFF)           RAM 32KB ($0000-$7FFF)
 ┌──────────────────┐              ┌──────────────────┐
 │A0-A14 ← A[14:0] │              │A0-A14 ← A[14:0] │
 │D0-D7  → D[7:0]  │              │D0-D7  ↔ D[7:0]  │
 │/CS    ← ROM_N   │              │/CS    ← RAM_N   │
 │/OE    ← RD_N    │              │/OE    ← RD_N    │
 │/WE    ← VCC     │              │/WE    ← WR_N    │
 └──────────────────┘              └──────────────────┘

═══════════════════════════════════════════════════════════════════════════════
 40-PIN EXPANSION CONNECTOR (J1)
═══════════════════════════════════════════════════════════════════════════════

  Pin  Signal    Pin  Signal
  ───  ───────   ───  ───────
   1   A0        2   A1
   3   A2        4   A3
   5   A4        6   A5
   7   A6        8   A7
   9   A8       10   A9
  11   A10      12   A11
  13   A12      14   A13
  15   A14      16   A15
  17   D0       18   D1
  19   D2       20   D3
  21   D4       22   D5
  23   D6       24   D7
  25   /RD      26   /WR
  27   CLK      28   /RST
  29   /NMI     30   /IRQ
  31   HALT     32   SYNC
  33   N/A      34   N/A
  35   N/A      36   N/A
  37   N/A      38   N/A
  39   VCC      40   GND
